[package]
name = "bliss-playlist-guidance-spi"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"
description = "Versioned service-provider interface for Bliss playlist guidance addons"
repository = "https://github.com/chrober/bliss-playlist-guidance-spi"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
